impl Solution {
    pub fn is_strictly_palindromic(n: i32) -> bool {
        // For n = 4, n in base_2 is 100 (Non-palindromic)
        // For n > 4, n in base_(n - 2) is 12 (Non-palindromic)
        return false;
    }
}
